# دليل إنشاء نظام "استمارة الفحص الفني الشامل" في Microsoft Access

هذا الدليل يشرح خطوة بخطوة كيفية بناء نظام فحص كامل في Access:
**جداول + علاقات + استعلام + نموذج إدخال + تقرير طباعة** (استمارة لكل قطعة).

> ملاحظة: يعمل Access على Windows. الملفات هنا (كود VBA + الدليل) تُنشئ النظام داخل Access لديك.

---

## الخطوة 1: إنشاء قاعدة بيانات جديدة
1. افتح Microsoft Access.
2. **New > Blank database** وسمِّها مثلاً `نظام_الفحص.accdb` واحفظها.

## الخطوة 2: بناء الجداول والعلاقات تلقائياً (كود جاهز)
1. اضغط **Alt + F11** لفتح محرر VBA.
2. من القائمة: **File ▸ Import File...** واختر الملف `اكسس_بناء_النظام.bas`.
   - أو: **Insert ▸ Module** ثم انسخ محتوى الملف والصقه.
3. من القائمة: **Run ▸ Run Sub/UserForm** (أو F5)، واختر الإجراء **`تشغيل_كامل`**.
4. ستظهر رسالة "اكتمل". الآن لديك:
   - الجداول: `tblالقطع` ، `tblالاستمارات` ، `tblالمراحل`
   - العلاقات بينها (واحد لمتعدد + حذف متتالٍ)
   - الاستعلام: `qryتفاصيل_الفحص`
   - بيانات تجريبية لقطعتين (P-1001 بثلاث مراحل، P-1002 بأربع مراحل)

> إذا ظهر تحذير أمان الماكرو: اضغط **Enable Content** ثم أعد التشغيل.

### بنية الجداول
```
tblالقطع
  رقم_القطعة  (نص، مفتاح أساسي)
  اسم_القطعة  (نص)

tblالاستمارات
  معرف_الاستمارة (ترقيم تلقائي، مفتاح)
  رقم_القطعة    (نص) ─────► يرتبط بـ tblالقطع
  اسم_المشروع   (نص)
  التاريخ       (نص)
  المجموعة      (نص)

tblالمراحل
  المعرف         (ترقيم تلقائي، مفتاح)
  معرف_الاستمارة (رقم) ─────► يرتبط بـ tblالاستمارات
  م              (رقم)   رقم المرحلة
  مرحلة_العمل    (نص)
  عدد_القطع      (رقم)
  الفني          (نص)
  القرار         (نص)   مقبول / مرفوض
  السماحية       (نص)
  السبب          (نص)
```

---

## الخطوة 3: نموذج الإدخال (رئيسي + فرعي)
هذا النموذج يتيح إدخال بيانات القطعة ومراحلها في شاشة واحدة.

1. في جزء التنقّل، اختر جدول **`tblالاستمارات`**.
2. تبويب **Create ▸ Form**. سيُنشئ Access نموذجاً وقد يضيف تلقائياً جدول المراحل كنموذج فرعي (بسبب العلاقة).
3. إن لم يظهر النموذج الفرعي:
   - افتح النموذج في **Design View**.
   - من **Design ▸ Subform/Subreport** ارسم مربعاً في أسفل النموذج، واختر جدول/استعلام **`tblالمراحل`**، واربط الحقول عبر **`معرف_الاستمارة`**.
4. لجعل الاتجاه من اليمين لليسار: افتح خصائص النموذج (F4) ▸ تبويب **Format** ▸ اضبط **Orientation = Right-to-Left**.
5. احفظ النموذج باسم `frmإدخال_الفحص`.

**لتحويل عمود القرار إلى قائمة منسدلة (مقبول/مرفوض):**
- في النموذج الفرعي، افتح Design View، حوّل حقل `القرار` إلى **Combo Box**.
- في خاصية **Row Source Type = Value List** وفي **Row Source** اكتب: `مقبول;مرفوض`

---

## الخطوة 4: تقرير الطباعة = استمارة الفحص لكل قطعة
هذا التقرير يطبع استمارة كاملة منسّقة لكل قطعة (بديل ملفات Word).

1. اختر الاستعلام **`qryتفاصيل_الفحص`**.
2. تبويب **Create ▸ Report Wizard**.
3. أضف كل الحقول، ثم عند "How do you want to view your data?" اختر التجميع حسب **`tblالاستمارات`** (أي حسب `معرف_الاستمارة`).
4. أضف مستوى تجميع إضافي إن رغبت، ثم رتّب التفاصيل حسب **`م`** تصاعدياً.
5. أنهِ المعالج واحفظ التقرير باسم `rptاستمارة_الفحص`.
6. افتحه في **Design View** ونسّقه:
   - **رأس المجموعة (Group Header)** ضع فيه: اسم القطعة، رقم القطعة، اسم المشروع، التاريخ، المجموعة، وشعار الجهة (Insert Image).
   - **قسم التفاصيل (Detail)**: الأعمدة م، مرحلة العمل، عدد القطع، الفني، القرار، السماحية، السبب.
   - **تذييل المجموعة (Group Footer)** ضع فيه مربعات نص للملخص:

```
إجمالي المراحل   =Count(*)
عدد المقبول      =Sum(IIf([القرار]="مقبول",1,0))
عدد المرفوض      =Sum(IIf([القرار]="مرفوض",1,0))
نسبة القبول      =Format(Sum(IIf([القرار]="مقبول",1,0))/Count(*),"0.0%")
```

   - أضف أسطر التواقيع: الضبط والجودة / مدير المشروع أو المنشأة / الفاحص الفني.
7. لاتجاه التقرير من اليمين لليسار: خصائص التقرير (F4) ▸ **Orientation = Right-to-Left**.
8. **لطباعة كل قطعة في صفحة مستقلة:** في خصائص **Group Footer** لمجموعة `معرف_الاستمارة` اضبط **Force New Page = After Section**.

**تلوين القرار (مقبول أخضر / مرفوض أحمر):**
- حدّد مربع نص `القرار` ▸ تبويب **Format ▸ Conditional Formatting**.
- قاعدة 1: `القرار = "مقبول"` ▸ خلفية خضراء.
- قاعدة 2: `القرار = "مرفوض"` ▸ خلفية حمراء.

---

## الخطوة 5: طباعة استمارة قطعة محددة
- افتح `rptاستمارة_الفحص` فيعرض كل القطع (كل قطعة في صفحتها).
- لطباعة قطعة واحدة فقط: أنشئ ماكرو/زر يفتح التقرير مع شرط تصفية، مثال في VBA:

```vba
DoCmd.OpenReport "rptاستمارة_الفحص", acViewPreview, , "[معرف_الاستمارة]=" & Me.معرف_الاستمارة
```

ضعه على زر داخل `frmإدخال_الفحص` باسم "طباعة الاستمارة".

---

## ملخص تدفّق العمل
```
إدخال البيانات في frmإدخال_الفحص
        │
        ▼
الجداول (القطع / الاستمارات / المراحل)
        │
        ▼
qryتفاصيل_الفحص  ──►  rptاستمارة_الفحص  ──►  طباعة استمارة لكل قطعة
```

## استيراد بياناتك الحقيقية
إن كان لديك بيانات في Excel (`بيانات_الفحص.xlsx`):
- **External Data ▸ New Data Source ▸ From File ▸ Excel** لاستيراد الصفوف إلى `tblالمراحل` وربطها بالاستمارات.
- أو أدخلها مباشرة عبر نموذج الإدخال.
